name: Secret Var Test


defaults:
  run:
    shell: pwsh


on:
  workflow_dispatch


jobs:
  test-secret-var:
    runs-on: ubuntu-latest
    steps:
      - name: Test Secret Var
        run: |
          if ('${{ secrets.TWITTER_ACCESS_TOKEN }}' -eq 'kristen') {
            Write-Host "Kristen";
          } else {
            Write-Host "Not Kristen";
          }
